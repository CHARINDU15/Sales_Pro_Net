<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".OutletsFragment">

    <!-- RecyclerView for displaying the list of outlets -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewOutlets"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="5dp"
        android:clipToPadding="false"
        android:paddingBottom="80dp"
        tools:listitem="@layout/item_outlet" />

    <!-- FloatingActionButton for adding a new outlet -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAddOutlet"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_margin="16dp"
        android:contentDescription="@string/add_outlet"
        android:src="@drawable/ic_add"
        app:backgroundTint="@color/apple15_green"
        app:fabSize="normal" />

</RelativeLayout>
